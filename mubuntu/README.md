Miranda Ubuntu Base image
=========================

We have an ubuntu mirror on Miranda. This is a docker base image that is configured to work with this mirror.
This will make docker image building significantly faster.
